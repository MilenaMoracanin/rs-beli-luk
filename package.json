{
  "name": "rs-beli-luk",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "scrape:bosut": "npm run scrape:bosut --workspace=web"
  }
}
